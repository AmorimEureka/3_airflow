prj_dw:
  outputs:
    dev:
      dbname: "{{ env_var('POSTGRES_DBNAME', 'prj_airflow') }}"
      host: localhost
      pass: "{{ env_var('POSTGRES_PASSWORD', '123') }}"
      port: "{{ env_var('POSTGRES_PORT', '5447') | int }}"
      schema: "{{ env_var('POSTGRES_SCHEMA', 'raw_dw') }}"
      threads: 4
      type: postgres
      user: "{{ env_var('POSTGRES_USER', 'db') }}"
  target: dev